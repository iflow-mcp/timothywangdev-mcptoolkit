[project]
name = "iflow-mcp_timothywangdev_fastmcp-multi-tenancy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.104.0",
    "mcp[cli]>=0.1.0",
    "redis>=5.0.1",
]

[project.scripts]
iflow-mcp-timothywangdev-fastmcp-multi-tenancy = "fastmcp_multi_tenancy.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fastmcp_multi_tenancy"]